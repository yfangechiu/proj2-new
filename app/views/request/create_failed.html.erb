<h1>Oh no! Your request could not be made :( </h1>
<h1>Please try again! </h1>

  <%= button_to('Try again', search_pets_path(user_id: current_user.id), :method => :get) %>

<br></br>

		<%= button_to("Return to Home Page", root_path, :class => "button medium", :method => :get) %> 
<br></br>

		  <%= button_to('My Profile', curr_user_path(user_id: current_user.id), :method => :get) %>

